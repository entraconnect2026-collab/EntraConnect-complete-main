const fs = require('fs'); const zlib = require('zlib'); const dir = 'C:/Users/balam/Downloads/Entra-Connect-Main/animated-world/non-animated-cloned/unity/desktop/Build/'; const files = ['desktop.data.br', 'desktop.framework.js.br', 'desktop.wasm.br']; for (const file of files) { const compressed = fs.readFileSync(dir + file); const decompressed = zlib.brotliDecompressSync(compressed); fs.writeFileSync(dir + file.replace('.br', ''), decompressed); console.log('Decompressed ' + file); }
